# HIGGS Quantization Requirements

# Core dependencies
llmcompressor  # Main library (should be installed from repo root)
transformers>=4.30.0
torch>=2.0.0

# ILP solver
pulp>=2.7.0

# Optional: For faster inference with quantized models
compressed-tensors

# Optional: GPU support
# - Install PyTorch with CUDA support from https://pytorch.org
